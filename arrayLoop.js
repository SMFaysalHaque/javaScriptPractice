var numbers = [44, 66, 33, 22, 11, 77];

for(var i = 0; i < numbers.length; i++){
    var element = numbers[i];
    // var element = numbers[2];
    // var element = numbers[numbers.length-1];
    console.log(element);
    // console.log(numbers[i]);
}